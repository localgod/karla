<cruisecontrol>
	<project name="Karla" buildafterfailed="false">
		<listeners>
			<currentbuildstatuslistener file="${status.file}" />
		</listeners>
		<bootstrappers>
			<gitbootstrapper localWorkingCopy="${project.dir}" />
		</bootstrappers>
		<modificationset quietperiod="${cruise.quietperiod}">
			<git localWorkingCopy="${project.dir}"/>
		</modificationset>
		<schedule interval="${cruise.interval}">
			<ant antscript="${ant.script}" buildfile="${project.dir}/build.xml"
				target="ccbuild" />
		</schedule>
		<log dir="${log.dir}">
			<merge dir="${project.dir}/build/logs/" />
		</log>
		<publishers>
			<execute command="phpuc graph --max-number 50 logs/${project.name} ${artifact.dir}" />
			<artifactspublisher dir="${project.dir}/build/api" dest="${artifact.dir}" subdirectory="api" />
			<artifactspublisher dir="${project.dir}/build/coverage" dest="${artifact.dir}" subdirectory="coverage" />
			<artifactspublisher dir="${project.dir}/build/php-code-browser" dest="${artifact.dir}" subdirectory="php-code-browser" />
			<artifactspublisher dir="${project.dir}/build/pdepend" dest="${artifact.dir}" subdirectory="graph" />
		</publishers>
	</project>
</cruisecontrol>
